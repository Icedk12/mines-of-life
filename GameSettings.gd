extends Node

var seed_ = randi() ## Used for map generation and random other stuff
var render_distance = 2 ## Chunk render distance, how many chunks far you can see
var chunk_size = 16  ## How big the chunks are.
