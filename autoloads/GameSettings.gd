extends Node

var seed_ : int = randi() ## Used for map generation and random other stuff
var render_distance : int = 3 ## Chunk render distance, how many chunks far you can see
var chunk_size : int = 16  ## How big the chunks are.
